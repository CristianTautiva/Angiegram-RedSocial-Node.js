<div class="container p-5">
    <div class="row">
        <div class="col-md-12">
            <div class="m-4">
            <div class="card text-center">
        <h5>Viendo Fotos del usuario seleccionado</h5>
        </div>
        </div>
    </div>
    {{#each photos}}

    <div class="col-md-4">
        <div class="card text-center">
            <div class="card-body">
                <a href="#">
                    <h3 class="card-title text-uppercase">
                        <a href="{{image_url}}" target="_blank"><img src="{{image_url}}" alt="{{public_id}}"
                                style="width:102%; height:60%;"></a>
                    </h3>
                </a>

                <p class="m-2"><b>publicada hace:</b> {{timeago fecha_subida}}</p>
                <p class="m-2"><b>Le gusta a:</b> {{ me_gusta }} personas</p>
                <a href="{{image_url}}" target="_blank" class="btn btn-primary">Me gusta</a>
                <a href="/links/report/{{id}}" class="btn btn-danger">Reportar </a>
            </div>
        </div>
    </div>
    {{else}}
    <div class="col-md-4 mx-auto">
        <div class="card card-body text-center">
            <p>Ups! Parece que no hay publicaciones aun, vuelve mas tarde</p><br>
            <a class="btn btn-success" href="/links/start">Volver al Inicio</a>
        </div>
    </div>

    {{/each}}

</div>
</div>
<style type="text/css">
    body {
        font-family: 'Oswald', sans-serif !important;
        overflow: auto !important;
    }
</style>